# Legacy Mojibake Archive

This folder preserves older handoff and log documents that were already mojibake-encoded in the repository.

They are kept for traceability, but they are not the source of truth for the current state.

Use these current documents instead:

- `../../CURRENT_STATUS.md`
- `../../merge/README.md`
- `../../merge/DIRECTION_THREE_HARDNEG_RESULTS.md`
